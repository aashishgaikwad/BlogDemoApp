on: push
name: npm lint, test
jobs:
  lint-and-test:
    name: build and publish
    runs-on: ubuntu-16.04
    steps:
      - uses: actions/checkout@master
      - name: npm install
        uses: actions/npm@master
        with:
          args: install
      - name: npm test
        uses: actions/npm@master
        with:
          args: test
      - name: npm lint
        uses: actions/npm@master
        with:
          args: run lint:fix
